# Marcador de pacote Python do backend FaciliChat.
# Sua existência permite importar os módulos de forma qualificada (ex: `from app.banco_dados import Base`)
# e executar a aplicação com `uvicorn app.main:app` a partir do diretório `backend/`.
